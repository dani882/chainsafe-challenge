name: Merge me
on:
  workflow_run:
    workflows: ["run-node-test"]
    types:
      - completed
jobs:
  merge-me:
    steps:
      - name: Merge me!
        uses: ridedott/merge-me-action@v2
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}